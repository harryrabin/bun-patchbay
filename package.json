{
  "name": "patchbay_v1",
  "version": "1.0.0",
  "description": "",
  "main": "launch.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -r ./dist && webpack",
    "launch:nobuild": "bun launch.ts",
    "launch": "bun run build && bun launch:nobuild"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^18.0.6",
    "bun-types": "^0.1.4",
    "cookie-interceptor": "^1.0.0",
    "copy-webpack-plugin": "^11.0.0",
    "webpack": "^5.74.0",
    "webpack-cli": "^4.10.0",
    "webpack-inject-plugin": "^1.5.5"
  }
}
